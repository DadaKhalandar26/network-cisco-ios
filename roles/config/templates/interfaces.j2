---
{% for intf in interfaces %}
interface {{ intf.name }}
{% if intf.description is defined %} description {{ intf.description }}{% endif %}
{% if intf.mtu is defined %} mtu {{ intf.mtu }}{% endif %}
{% if intf.ip is defined %} ip address {{ intf.ip }}{% endif %}
{% if intf.shutdown | default(false) %} shutdown{% endif %}
!
{% endfor %}
